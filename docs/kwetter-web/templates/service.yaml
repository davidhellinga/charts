apiVersion: v1
kind: Service
metadata:
  name: {{ include "kwetter-web.fullname" . }}
  labels:
    {{- include "kwetter-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kwetter-web.selectorLabels" . | nindent 4 }}
